# Optional model stack — only needed for HEURISTIC_MODE=false.
#
# These are large (torch alone is ~200MB) and the first run downloads
# distilbert-base-uncased-finetuned-sst-2-english (~250MB) from Hugging Face,
# so this file is deliberately NOT part of the default install.
#
#   pip install -r requirements.txt -r requirements-models.txt
#   HEURISTIC_MODE=false npm run dev:ml
#
# Requires Python 3.11 or 3.12 — torch has no wheels for 3.13+ at time of writing.
numpy>=1.26,<3
scikit-learn>=1.4
torch>=2.2
transformers>=4.40
