# Core — everything the service needs to run in HEURISTIC_MODE (the default).
#
# Deliberately unpinned (lower bounds only) so pip can resolve wheels for whatever
# interpreter is present. Hard pins broke on Python 3.14, where pydantic-core has
# no prebuilt wheel and falls back to a Rust source build. The demo must never
# depend on a working Rust toolchain.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
python-dotenv>=1.0

# For the real models, additionally install:
#   pip install -r requirements-models.txt
# then set HEURISTIC_MODE=false. See services/ml/README.md.
