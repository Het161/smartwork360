# --- always required (heuristic mode runs on these alone) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# --- required for HEURISTIC_MODE=false (real models) ---
# Kept in the same file so `pip install -r requirements.txt` gives the full
# experience; the service degrades to heuristics automatically if imports fail.
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
torch>=2.2
transformers>=4.40
